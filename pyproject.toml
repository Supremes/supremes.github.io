[project]
name = "knowledge-base"
version = "0.1.0"
description = "Personal knowledge base, static-built from Markdown for GitHub Pages."
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "markdown>=3.5",
    "pygments>=2.17",
    "pyyaml>=6.0",
]
